[build]
builder = "nixpacks"
buildCommand = "pnpm turbo build --filter=@calibr/api"

[deploy]
startCommand = "pnpm --filter @calibr/api start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[service]
internalPort = 3001
