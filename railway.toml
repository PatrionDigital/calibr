[build]
builder = "nixpacks"
buildCommand = "pnpm turbo build --filter=@calibr/api"

[deploy]
startCommand = "cd packages/api && node dist/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[service]
internalPort = 3001
